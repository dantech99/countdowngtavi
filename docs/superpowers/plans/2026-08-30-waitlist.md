# Lista de espera — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar a la home una sección con un botón "me sumo" y un contador global de cuánta gente se sumó al lanzamiento, sin pedir email ni ningún dato personal.

**Architecture:** El dominio y el manejo HTTP viven como funciones puras en `src/lib/`, testeadas sin Astro ni red. Un único endpoint (`src/pages/api/waitlist.ts`) se renderiza on-demand con el adapter de Vercel y delega en esas funciones; el resto del sitio se sigue prerenderizando igual que hoy. La persistencia es un solo set de Redis en Upstash, donde `SADD` resuelve inserción y deduplicación en una operación atómica y el contador es el cardinal del set.

**Tech Stack:** Astro 7 (`output: 'static'` con una ruta on-demand), `@astrojs/vercel` 11, `@upstash/redis` 1.38, Tailwind v4, GSAP, Vitest.

**Spec:** `docs/superpowers/specs/2026-08-30-waitlist-design.md`

## Global Constraints

- **Idioma del código:** comentarios en inglés (como todo el repo actual); todo el texto visible al usuario en español.
- **Commits:** conventional commits, en español, **sin `Co-Authored-By` ni atribución de IA**.
- **Clave de Redis:** exactamente `waitlist:members`. Nunca un segundo contador aparte.
- **Clave de localStorage:** exactamente `gta6:waitlist`, siguiendo la convención de `gta6:music`.
- **Todo acceso a `localStorage` va envuelto en `try/catch`.** La navegación privada lo rechaza y el botón debe seguir funcionando.
- **Las credenciales de Upstash se declaran `optional: true`** en el schema de `astro:env`. Si fueran requeridas, un clon sin `.env` no compilaría.
- **Nada bajo `src/lib/` importa `astro:env/server`.** Vitest no resuelve ese módulo virtual y el test fallaría al cargar.
- **Fuera de alcance:** emails, rate limiting por IP, captcha, panel de administración.
- **Formato del número:** `Intl.NumberFormat('es-CO')` — separador de miles con punto (`1.247`, no `1,247`).
- **Comandos:** el gestor de paquetes es `pnpm`. Los tests corren con `pnpm test` (`vitest run`).

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `src/lib/waitlist.js` | Dominio: validar el id, operar sobre un store abstracto `{ sadd, scard }`, formatear el contador. Sin red, sin DOM, sin Astro. |
| `src/lib/waitlist.test.js` | Tests del dominio contra un store en memoria. |
| `src/lib/waitlist-http.js` | Traducción HTTP: métodos, parseo, validación, códigos de estado, cabeceras. `Request` → `Response`. |
| `src/lib/waitlist-http.test.js` | Tests del handler con `Request` reales. |
| `src/pages/api/waitlist.ts` | Cableado: lee credenciales, construye el cliente Upstash, delega. Tan delgado que no tiene test propio. |
| `src/components/Waitlist.astro` | Markup, estilos y script de cliente de la sección. |
| `astro.config.mjs` | Adapter de Vercel y schema de `astro:env`. |
| `src/pages/index.astro` | Monta la sección tras el hero, con su animación de entrada. |
| `src/components/Footer.astro` | Enlace `#waitlist` en la navegación del pie. |

La división entre `waitlist.js` y `waitlist-http.js` no es ceremonia: el primero no sabe qué es una petición HTTP y el segundo no sabe qué es Redis. Cada uno se puede leer y testear entero por separado.

---

### Task 1: Dominio de la lista de espera

**Files:**
- Create: `src/lib/waitlist.js`
- Test: `src/lib/waitlist.test.js`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `MEMBERS_KEY: string` — la constante `'waitlist:members'`.
  - `isValidMemberId(id: unknown): boolean`
  - `getCount(store): Promise<number>`
  - `joinWaitlist(store, id: string): Promise<{ count: number, joined: boolean }>` — lanza `Error('invalid_id')` si el id no es válido.
  - `formatCount(n: number): { value: string, label: string }`
  - El `store` es cualquier objeto con `sadd(key, member): Promise<number>` (número de miembros nuevos insertados: 1 o 0) y `scard(key): Promise<number>`.

- [ ] **Step 1: Write the failing test**

Crear `src/lib/waitlist.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { isValidMemberId, getCount, joinWaitlist, formatCount, MEMBERS_KEY } from './waitlist.js';

// Mirrors the two Redis commands the real store provides, including SADD's
// return value: the number of members actually added, which is what makes
// deduplication a single atomic call instead of a read followed by a write.
function createFakeStore() {
  const sets = new Map();
  return {
    async sadd(key, member) {
      const set = sets.get(key) ?? new Set();
      sets.set(key, set);
      const before = set.size;
      set.add(member);
      return set.size - before;
    },
    async scard(key) {
      return sets.get(key)?.size ?? 0;
    },
  };
}

const VALID_ID = '3f2504e0-4f89-41d3-9a0c-0305e82c3301';

describe('isValidMemberId', () => {
  it('accepts a v4 UUID', () => {
    expect(isValidMemberId(VALID_ID)).toBe(true);
  });

  it('rejects a UUID of a different version', () => {
    expect(isValidMemberId('3f2504e0-4f89-11d3-9a0c-0305e82c3301')).toBe(false);
  });

  it('rejects arbitrary strings', () => {
    expect(isValidMemberId('not-a-uuid')).toBe(false);
    expect(isValidMemberId('')).toBe(false);
  });

  it('rejects values that are not strings', () => {
    expect(isValidMemberId(undefined)).toBe(false);
    expect(isValidMemberId(null)).toBe(false);
    expect(isValidMemberId(42)).toBe(false);
  });
});

describe('getCount', () => {
  it('returns zero for an empty store', async () => {
    await expect(getCount(createFakeStore())).resolves.toBe(0);
  });

  it('reads the members key', async () => {
    const store = createFakeStore();
    await store.sadd(MEMBERS_KEY, VALID_ID);
    await expect(getCount(store)).resolves.toBe(1);
  });
});

describe('joinWaitlist', () => {
  it('adds a new member and reports the join', async () => {
    const store = createFakeStore();
    await expect(joinWaitlist(store, VALID_ID)).resolves.toEqual({ count: 1, joined: true });
  });

  // The whole design rests on this: a visitor who clicks twice, reloads, or
  // retries a failed request must never inflate the public number.
  it('does not count the same id twice', async () => {
    const store = createFakeStore();
    await joinWaitlist(store, VALID_ID);
    await expect(joinWaitlist(store, VALID_ID)).resolves.toEqual({ count: 1, joined: false });
  });

  it('counts distinct ids separately', async () => {
    const store = createFakeStore();
    await joinWaitlist(store, VALID_ID);
    await joinWaitlist(store, '9c858901-8a57-4791-81fe-4c455b099bc9');
    await expect(getCount(store)).resolves.toBe(2);
  });

  it('rejects an invalid id without touching the store', async () => {
    const store = createFakeStore();
    await expect(joinWaitlist(store, 'not-a-uuid')).rejects.toThrow('invalid_id');
    await expect(getCount(store)).resolves.toBe(0);
  });
});

describe('formatCount', () => {
  it('uses the singular for one member', () => {
    expect(formatCount(1)).toEqual({ value: '1', label: 'persona ya se sumó' });
  });

  it('uses the plural for zero and for many', () => {
    expect(formatCount(0)).toEqual({ value: '0', label: 'personas ya se sumaron' });
    expect(formatCount(2)).toEqual({ value: '2', label: 'personas ya se sumaron' });
  });

  // es-CO groups thousands with a period, not a comma.
  it('groups thousands the Colombian way', () => {
    expect(formatCount(1247).value).toBe('1.247');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run src/lib/waitlist.test.js`
Expected: FAIL — no se puede resolver `./waitlist.js`.

- [ ] **Step 3: Write minimal implementation**

Crear `src/lib/waitlist.js`:

```js
export const MEMBERS_KEY = 'waitlist:members';

const UUID_V4 = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;

const COUNT_FORMAT = new Intl.NumberFormat('es-CO');

export function isValidMemberId(id) {
  return typeof id === 'string' && UUID_V4.test(id);
}

export async function getCount(store) {
  return await store.scard(MEMBERS_KEY);
}

// SADD returns how many members it actually added, so a repeated id comes back
// as 0 without a separate lookup. The count is the set's cardinality rather
// than a counter of its own: with one source of truth there is no pair of
// values that can drift apart.
export async function joinWaitlist(store, id) {
  if (!isValidMemberId(id)) {
    throw new Error('invalid_id');
  }

  const added = await store.sadd(MEMBERS_KEY, id);
  const count = await store.scard(MEMBERS_KEY);

  return { count, joined: added === 1 };
}

// Returned in two pieces because the section styles them differently: the
// number carries the GTA outline, the caption the softer variant.
export function formatCount(n) {
  return {
    value: COUNT_FORMAT.format(n),
    label: n === 1 ? 'persona ya se sumó' : 'personas ya se sumaron',
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run src/lib/waitlist.test.js`
Expected: PASS — 13 tests.

- [ ] **Step 5: Run the whole suite to check for regressions**

Run: `pnpm test`
Expected: PASS — los tests de `countdown`, `news`, `rss`, `trailers` y `ambient-music` siguen verdes.

- [ ] **Step 6: Commit**

```bash
git add src/lib/waitlist.js src/lib/waitlist.test.js
git commit -m "feat: agregar dominio de lista de espera con deduplicacion por id"
```

---

### Task 2: Handler HTTP

**Files:**
- Create: `src/lib/waitlist-http.js`
- Test: `src/lib/waitlist-http.test.js`

**Interfaces:**
- Consumes: de Task 1 — `isValidMemberId`, `getCount`, `joinWaitlist`.
- Produces: `handleWaitlistRequest(request: Request, store): Promise<Response>`, donde `store` puede ser `null` para señalar que las credenciales no están configuradas.

Este archivo es la única pieza que conoce códigos de estado y cabeceras. No importa nada de Astro ni de Upstash, así que se testea con `Request` y `Response` estándar de Node 22.

- [ ] **Step 1: Write the failing test**

Crear `src/lib/waitlist-http.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { handleWaitlistRequest } from './waitlist-http.js';
import { MEMBERS_KEY } from './waitlist.js';

function createFakeStore() {
  const sets = new Map();
  return {
    async sadd(key, member) {
      const set = sets.get(key) ?? new Set();
      sets.set(key, set);
      const before = set.size;
      set.add(member);
      return set.size - before;
    },
    async scard(key) {
      return sets.get(key)?.size ?? 0;
    },
  };
}

const URL = 'https://example.com/api/waitlist';
const VALID_ID = '3f2504e0-4f89-41d3-9a0c-0305e82c3301';

function postRequest(body) {
  return new Request(URL, {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body,
  });
}

describe('handleWaitlistRequest — GET', () => {
  it('returns the current count', async () => {
    const store = createFakeStore();
    await store.sadd(MEMBERS_KEY, VALID_ID);

    const res = await handleWaitlistRequest(new Request(URL), store);

    expect(res.status).toBe(200);
    await expect(res.json()).resolves.toEqual({ count: 1 });
  });

  // Lets Vercel's edge serve the number instead of waking a function on every
  // single page view.
  it('allows the edge to cache the number briefly', async () => {
    const res = await handleWaitlistRequest(new Request(URL), createFakeStore());
    expect(res.headers.get('cache-control')).toBe('public, s-maxage=30');
  });

  it('reports 502 when the store fails', async () => {
    const broken = {
      async sadd() { throw new Error('upstash down'); },
      async scard() { throw new Error('upstash down'); },
    };

    const res = await handleWaitlistRequest(new Request(URL), broken);

    expect(res.status).toBe(502);
    await expect(res.json()).resolves.toEqual({ error: 'upstream' });
  });
});

describe('handleWaitlistRequest — POST', () => {
  it('joins a new member', async () => {
    const res = await handleWaitlistRequest(
      postRequest(JSON.stringify({ id: VALID_ID })),
      createFakeStore(),
    );

    expect(res.status).toBe(200);
    await expect(res.json()).resolves.toEqual({ count: 1, joined: true });
  });

  it('reports joined: false when the id is already in the set', async () => {
    const store = createFakeStore();
    await handleWaitlistRequest(postRequest(JSON.stringify({ id: VALID_ID })), store);

    const res = await handleWaitlistRequest(postRequest(JSON.stringify({ id: VALID_ID })), store);

    await expect(res.json()).resolves.toEqual({ count: 1, joined: false });
  });

  it('rejects malformed JSON', async () => {
    const res = await handleWaitlistRequest(postRequest('{ not json'), createFakeStore());

    expect(res.status).toBe(400);
    await expect(res.json()).resolves.toEqual({ error: 'invalid_id' });
  });

  it('rejects a body without an id', async () => {
    const res = await handleWaitlistRequest(postRequest(JSON.stringify({})), createFakeStore());
    expect(res.status).toBe(400);
  });

  it('rejects an id that is not a v4 UUID', async () => {
    const res = await handleWaitlistRequest(
      postRequest(JSON.stringify({ id: 'not-a-uuid' })),
      createFakeStore(),
    );
    expect(res.status).toBe(400);
  });

  it('rejects an oversized body', async () => {
    const res = await handleWaitlistRequest(
      postRequest(JSON.stringify({ id: VALID_ID, padding: 'x'.repeat(2000) })),
      createFakeStore(),
    );
    expect(res.status).toBe(400);
  });
});

describe('handleWaitlistRequest — other cases', () => {
  it('rejects unsupported methods', async () => {
    const res = await handleWaitlistRequest(new Request(URL, { method: 'DELETE' }), createFakeStore());

    expect(res.status).toBe(405);
    await expect(res.json()).resolves.toEqual({ error: 'method_not_allowed' });
  });

  // A clone without credentials must answer honestly instead of crashing.
  it('reports 503 when no store is configured', async () => {
    const res = await handleWaitlistRequest(new Request(URL), null);

    expect(res.status).toBe(503);
    await expect(res.json()).resolves.toEqual({ error: 'unavailable' });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run src/lib/waitlist-http.test.js`
Expected: FAIL — no se puede resolver `./waitlist-http.js`.

- [ ] **Step 3: Write minimal implementation**

Crear `src/lib/waitlist-http.js`:

```js
import { getCount, isValidMemberId, joinWaitlist } from './waitlist.js';

// Enough room for the JSON envelope around a UUID and nothing else. The real
// payload cap is Vercel's; this only keeps an obviously bogus body from
// reaching JSON.parse.
const MAX_BODY_CHARS = 1024;

function json(data, status = 200, headers = {}) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { 'content-type': 'application/json', ...headers },
  });
}

async function handleGet(store) {
  try {
    const count = await getCount(store);
    return json({ count }, 200, { 'cache-control': 'public, s-maxage=30' });
  } catch {
    return json({ error: 'upstream' }, 502);
  }
}

async function handlePost(request, store) {
  const raw = await request.text();
  if (raw.length > MAX_BODY_CHARS) {
    return json({ error: 'invalid_id' }, 400);
  }

  let body;
  try {
    body = JSON.parse(raw);
  } catch {
    return json({ error: 'invalid_id' }, 400);
  }

  if (!isValidMemberId(body?.id)) {
    return json({ error: 'invalid_id' }, 400);
  }

  try {
    return json(await joinWaitlist(store, body.id));
  } catch {
    return json({ error: 'upstream' }, 502);
  }
}

export async function handleWaitlistRequest(request, store) {
  if (!store) {
    return json({ error: 'unavailable' }, 503);
  }

  if (request.method === 'GET') {
    return await handleGet(store);
  }

  if (request.method === 'POST') {
    return await handlePost(request, store);
  }

  return json({ error: 'method_not_allowed' }, 405);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run src/lib/waitlist-http.test.js`
Expected: PASS — 11 tests.

- [ ] **Step 5: Run the whole suite**

Run: `pnpm test`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/lib/waitlist-http.js src/lib/waitlist-http.test.js
git commit -m "feat: agregar handler http de la lista de espera"
```

---

### Task 3: Endpoint on-demand y configuración de Astro

**Files:**
- Modify: `astro.config.mjs` (archivo completo, 11 líneas)
- Create: `src/pages/api/waitlist.ts`
- Modify: `package.json` (vía `pnpm add`)

**Interfaces:**
- Consumes: de Task 2 — `handleWaitlistRequest(request, store)`.
- Produces: las rutas HTTP `GET /api/waitlist` y `POST /api/waitlist`, que Task 4 consume desde el navegador.

- [ ] **Step 1: Provision Upstash Redis (manual, requiere cuenta)**

Este paso lo ejecuta la persona dueña del proyecto, no un agente:

```bash
vercel integration add upstash
```

Elegir el producto Redis. Vercel conecta el recurso al proyecto e inyecta `KV_REST_API_URL` y `KV_REST_API_TOKEN` en el entorno.

Luego, para desarrollo local, copiar esas dos variables a un `.env` en la raíz (`.env` ya está en `.gitignore`):

```
KV_REST_API_URL=https://...
KV_REST_API_TOKEN=...
```

Si este paso todavía no se hizo, el resto de la tarea se puede completar igual: el endpoint responderá 503 y ese es justamente uno de los casos de verificación.

- [ ] **Step 2: Install the dependencies**

```bash
pnpm add @astrojs/vercel @upstash/redis
```

- [ ] **Step 3: Update the Astro config**

Reemplazar el contenido completo de `astro.config.mjs`:

```js
// @ts-check
import { defineConfig, envField } from 'astro/config';
import vercel from '@astrojs/vercel';

import tailwindcss from '@tailwindcss/vite';

// https://astro.build/config
export default defineConfig({
  adapter: vercel(),
  // Declared optional on purpose: a clone without an .env still has to build.
  // The endpoint answers 503 at runtime instead of the build failing outright.
  env: {
    schema: {
      KV_REST_API_URL: envField.string({ context: 'server', access: 'secret', optional: true }),
      KV_REST_API_TOKEN: envField.string({ context: 'server', access: 'secret', optional: true }),
    },
  },
  vite: {
    plugins: [tailwindcss()]
  }
});
```

- [ ] **Step 4: Write the endpoint**

Crear `src/pages/api/waitlist.ts`:

```ts
import type { APIRoute } from 'astro';
import { Redis } from '@upstash/redis';
import { KV_REST_API_URL, KV_REST_API_TOKEN } from 'astro:env/server';
import { handleWaitlistRequest } from '../../lib/waitlist-http.js';

// The only route in the project that is not prerendered.
export const prerender = false;

// Built once per function instance rather than per request: the Upstash client
// speaks HTTP, so there is no connection to open, keep alive, or tear down.
// Missing credentials leave it null, which the handler turns into a 503.
const store =
  KV_REST_API_URL && KV_REST_API_TOKEN
    ? new Redis({ url: KV_REST_API_URL, token: KV_REST_API_TOKEN })
    : null;

// ALL rather than separate GET/POST exports: it lets the handler answer 405
// itself, instead of Astro turning an unsupported method into a 404.
export const ALL = (({ request }) => handleWaitlistRequest(request, store)) satisfies APIRoute;
```

- [ ] **Step 5: Verify the endpoint by hand**

```bash
pnpm astro dev --background
```

Sin credenciales en `.env`:

```bash
curl -s -i localhost:4321/api/waitlist | head -1
# Expected: HTTP/1.1 503
```

Con credenciales en `.env` (reiniciar el dev server después de crearlo):

```bash
# Un id inválido es rechazado
curl -s -X POST localhost:4321/api/waitlist \
  -H 'content-type: application/json' -d '{"id":"not-a-uuid"}'
# Expected: {"error":"invalid_id"}

# Un id válido entra
curl -s -X POST localhost:4321/api/waitlist \
  -H 'content-type: application/json' \
  -d '{"id":"3f2504e0-4f89-41d3-9a0c-0305e82c3301"}'
# Expected: {"count":1,"joined":true}

# El mismo id no vuelve a contar
curl -s -X POST localhost:4321/api/waitlist \
  -H 'content-type: application/json' \
  -d '{"id":"3f2504e0-4f89-41d3-9a0c-0305e82c3301"}'
# Expected: {"count":1,"joined":false}

# La lectura coincide
curl -s localhost:4321/api/waitlist
# Expected: {"count":1}

# Un método no soportado
curl -s -X DELETE localhost:4321/api/waitlist
# Expected: {"error":"method_not_allowed"}
```

- [ ] **Step 6: Verify the rest of the site is still prerendered**

```bash
pnpm astro dev stop
pnpm build
find dist -name '*.html' | sort
```

Expected: siguen apareciendo `index.html`, `noticias/index.html` y el HTML de cada artículo. Si el build dejó de emitir HTML estático, el adapter se configuró mal — el proyecto debe seguir en `output: 'static'`, sin `output: 'server'`.

- [ ] **Step 7: Commit**

```bash
git add astro.config.mjs package.json pnpm-lock.yaml src/pages/api/waitlist.ts
git commit -m "feat: agregar endpoint on-demand de lista de espera con upstash"
```

---

### Task 4: Sección de la lista de espera en la home

**Files:**
- Create: `src/components/Waitlist.astro`
- Modify: `src/pages/index.astro` (import y montaje tras `<Hero />`, más la animación de entrada en el `<script>` del final)
- Modify: `src/components/Footer.astro` (un enlace más en la navegación)

**Interfaces:**
- Consumes: de Task 1 — `formatCount(n)`. De Task 3 — `GET`/`POST` en `/api/waitlist`.
- Produces: la sección `#waitlist` en la home. Nada depende de ella.

- [ ] **Step 1: Create the component**

Crear `src/components/Waitlist.astro`:

```astro
---
// src/components/Waitlist.astro
---

<section id="waitlist" class="mx-auto max-w-2xl px-4 py-20 text-center">
	<h2 class="text-2xl font-black uppercase">Súmate al lanzamiento</h2>
	<p class="mt-4 text-white/70">Marca que vas a estar el día uno. Sin email, sin registro.</p>

	<div class="mt-8" data-waitlist-count aria-live="polite" hidden>
		<p class="gta-outline text-5xl font-black tabular-nums" data-waitlist-value>—</p>
		<p class="gta-outline-soft mt-1 text-xs uppercase tracking-widest text-white/80" data-waitlist-label>
			personas ya se sumaron
		</p>
	</div>

	<button
		id="waitlist-button"
		type="button"
		data-state="idle"
		disabled
		class="mt-8 rounded-full border border-white/20 px-8 py-3 text-sm font-black uppercase tracking-wide transition-colors hover:border-gta-pink hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white disabled:cursor-not-allowed data-[state=joined]:border-gta-pink"
	>
		Me sumo
	</button>

	<p class="mt-4 h-5 text-sm text-white/60" role="status" data-waitlist-error></p>
</section>

<script>
	import { gsap } from 'gsap';
	import { formatCount } from '../lib/waitlist.js';

	const STORAGE_KEY = 'gta6:waitlist';

	const button = document.getElementById('waitlist-button') as HTMLButtonElement | null;

	if (button) {
		const section = document.getElementById('waitlist')!;
		const counter = section.querySelector('[data-waitlist-count]') as HTMLElement;
		const valueEl = section.querySelector('[data-waitlist-value]') as HTMLElement;
		const labelEl = section.querySelector('[data-waitlist-label]') as HTMLElement;
		const errorEl = section.querySelector('[data-waitlist-error]') as HTMLElement;

		// Same treatment the countdown gives its digits, and the same guard the
		// hero uses for its parallax: matchMedia keeps the motion off for anyone
		// who asked for reduced motion, and reverts it if that changes.
		let bump = () => {};
		const mm = gsap.matchMedia();
		mm.add('(prefers-reduced-motion: no-preference)', () => {
			bump = () => {
				gsap.fromTo(
					valueEl,
					{ scale: 1.3, opacity: 0.4 },
					{ scale: 1, opacity: 1, duration: 0.35, ease: 'power2.out' },
				);
			};
			return () => {
				bump = () => {};
			};
		});

		function readId(): string | null {
			try {
				return localStorage.getItem(STORAGE_KEY);
			} catch {
				// Private browsing can refuse storage; the button still works, it
				// just will not remember that this visitor already joined.
				return null;
			}
		}

		function rememberId(id: string) {
			try {
				localStorage.setItem(STORAGE_KEY, id);
			} catch {
				// Same as above: failing to remember is fine, throwing is not.
			}
		}

		function render(state: 'idle' | 'sending' | 'joined') {
			button!.dataset.state = state;
			button!.disabled = state !== 'idle';
			button!.textContent =
				state === 'sending' ? 'Sumándote…' : state === 'joined' ? 'Ya estás dentro ✓' : 'Me sumo';
		}

		function showCount(count: number) {
			const { value, label } = formatCount(count);
			valueEl.textContent = value;
			labelEl.textContent = label;
			counter.hidden = false;
			bump();
		}

		// The markup ships the button disabled so that a visitor without
		// JavaScript never sees a control that silently does nothing.
		render(readId() ? 'joined' : 'idle');

		// The number is nice to have, not load-bearing: if it cannot be read the
		// counter stays hidden rather than showing a zero that is not true.
		fetch('/api/waitlist')
			.then((res) => (res.ok ? res.json() : Promise.reject(new Error('unavailable'))))
			.then((data) => showCount(data.count))
			.catch(() => {});

		button.addEventListener('click', async () => {
			errorEl.textContent = '';
			render('sending');

			const id = readId() ?? crypto.randomUUID();

			try {
				const res = await fetch('/api/waitlist', {
					method: 'POST',
					headers: { 'content-type': 'application/json' },
					body: JSON.stringify({ id }),
				});
				if (!res.ok) throw new Error('join failed');

				const data = await res.json();
				rememberId(id);
				showCount(data.count);
				render('joined');
			} catch {
				// Nothing is remembered on failure, so the next click retries with
				// the same id and the count still cannot drift.
				render('idle');
				errorEl.textContent = 'No pudimos sumarte. Intenta de nuevo.';
			}
		});
	}
</script>
```

- [ ] **Step 2: Mount the section on the home page**

En `src/pages/index.astro`, agregar el import junto a los demás componentes:

```astro
import Waitlist from '../components/Waitlist.astro';
```

Montarlo inmediatamente después del hero:

```astro
	<Hero />

	<Waitlist />

	<section id="trailers" class="mx-auto max-w-5xl px-4 pt-20">
```

Y en el `<script>` del final del archivo, agregar la animación de entrada junto a las de `#trailers` y `#news-preview`:

```js
	gsap.from('#waitlist', {
		opacity: 0,
		y: 32,
		duration: 0.8,
		ease: 'power2.out',
		scrollTrigger: {
			trigger: '#waitlist',
			start: 'top 80%',
		},
	});
```

- [ ] **Step 3: Add the footer link**

En `src/components/Footer.astro`, dentro del segundo `<p>`, agregar el enlace entre "Noticias" y "Donaciones":

```astro
		<a href="/noticias" class="underline hover:text-white">Noticias</a>
		<span class="mx-2">·</span>
		<a href="/#waitlist" class="underline hover:text-white">Lista de espera</a>
		<span class="mx-2">·</span>
		<a href="#donaciones" class="underline hover:text-white">Donaciones</a>
```

El `href` lleva `/` delante porque el footer también se renderiza en `/noticias`, donde un ancla suelta no encontraría la sección.

- [ ] **Step 4: Verify in the browser**

```bash
pnpm astro dev --background
```

Abrir `localhost:4321` y comprobar, en orden:

1. La sección aparece justo debajo del hero, con la animación de entrada al scrollear.
2. El contador muestra el número real (no `—`) una vez que carga.
3. Al hacer clic: el botón pasa a "Sumándote…", luego a "Ya estás dentro ✓" con el borde rosa, y el número sube en uno.
4. Recargar la página: el botón sigue en "Ya estás dentro ✓" y el número **no** volvió a subir.
5. En una ventana de incógnito el botón vuelve a aparecer habilitado.
6. Con las DevTools en modo offline, recargar: la sección se muestra sin el bloque del número, y el clic deja el mensaje "No pudimos sumarte. Intenta de nuevo." con el botón habilitado otra vez.
7. Con `prefers-reduced-motion: reduce` activado en las DevTools, el número aparece sin la animación de escala.

- [ ] **Step 5: Verify the build and the suite**

```bash
pnpm astro dev stop
pnpm test
pnpm build
```

Expected: los tests pasan y el build termina sin errores, con el HTML estático de la home y de noticias en `dist/`.

- [ ] **Step 6: Commit**

```bash
git add src/components/Waitlist.astro src/components/Footer.astro src/pages/index.astro
git commit -m "feat: agregar seccion de lista de espera con contador en la home"
```

---

## Verificación final

Después de la Task 4, con el sitio desplegado:

- [ ] `GET /api/waitlist` en producción devuelve un número y la cabecera `Cache-Control: public, s-maxage=30`.
- [ ] Sumarse desde dos navegadores distintos incrementa el número dos veces; recargar en cualquiera de ellos no lo mueve.
- [ ] La home, `/noticias` y los artículos se siguen sirviendo como HTML estático.
- [ ] El workflow `rebuild-news.yml` sigue disparando su deploy hook sin cambios.
