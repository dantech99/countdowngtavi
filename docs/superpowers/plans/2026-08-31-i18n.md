# Internacionalización es/en — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Servir countdowngtavi.com en español e inglés, con un switch manual en un header fijo y detección automática del idioma del navegador en la primera visita a la raíz.

**Architecture:** El español vive sin prefijo y el inglés bajo `/en/`. Las rutas se generan con un rest param opcional `[...locale]`, así que hay un solo archivo por página en vez de uno por idioma. El idioma se deriva siempre de la URL con un helper propio con tests (`getLangFromUrl`), y los textos salen de un diccionario tipado donde una clave faltante en inglés es un error de compilación. Como el sitio es 100 % estático, la detección se resuelve con un script inline bloqueante en la home, no con middleware.

**Tech Stack:** Astro 7.2.9 (static + adapter Vercel), Tailwind v4, GSAP/ScrollTrigger, vitest 4, TypeScript 6.

**Spec:** `docs/superpowers/specs/2026-08-31-i18n-design.md`

## Global Constraints

- Idiomas: `es` (default, sin prefijo) y `en` (bajo `/en/`). Ningún otro.
- `site` es exactamente `https://countdowngtavi.com`.
- Las URLs existentes no cambian: `/`, `/noticias`, `/noticias/bienvenida` siguen resolviendo igual.
- Clave de `localStorage` para el idioma: `gta6:lang`. Valores: `es` o `en`. Solo el switch escribe; la detección nunca escribe.
- Todo acceso a `localStorage` va dentro de `try/catch` — el modo privado puede lanzar. Es el patrón que ya usan `MusicToggle.astro` y `Waitlist.astro`.
- `Layout.astro` no recibe props nuevas. El último commit del repo sacó una prop a propósito.
- AdSense y `@vercel/analytics/astro` en `Layout.astro` se preservan sin tocar.
- Los nombres de trailers en `src/lib/trailers.js` (`"An Extended Look"`, `"Trailer 2"`, `"Trailer 1"`) son títulos oficiales de Rockstar: **no se traducen**.
- Los feeds RSS son en inglés y se muestran igual en ambos idiomas. No se filtran ni se traducen.
- Nunca se emite `hreflang` hacia una traducción que no existe.
- Archivos que este plan NO toca: `src/pages/api/waitlist.ts`, `src/components/TrailerPoster.astro`, `src/lib/countdown.js`, `src/lib/news.js`, `src/lib/rss.js`, `src/lib/trailers.js`, `src/lib/ambient-music.js`, `src/lib/waitlist-http.js`.

## Desviación respecto del spec

El spec dice `src/i18n/utils.js`. Este plan usa **`.ts`** para los tres archivos de `src/i18n/`.
Razón: la garantía central del diseño — que una clave faltante en inglés sea un error de
compilación — solo existe con tipos, y un `.js` importando un `.ts` obliga a decidir entre una
extensión que TypeScript rechaza (`./ui.ts`) o una que solo resuelve por convención de Vite
(`./ui.js` apuntando a un `.ts`). El proyecto ya tiene TypeScript y ya usa `.ts` donde los
tipos aportan (`src/pages/api/waitlist.ts`). `src/lib/articulos.js` sí se queda en `.js`, como
sus vecinos, porque ahí los tipos no aportan nada.

## Nota sobre commits y verificación

El dueño del repo hace los commits, corre los tests y ejecuta los builds. Cada tarea termina
en un **Checkpoint** que dice qué comando verifica el trabajo y qué commit sugerir — pero
**no lo ejecutes ni lo commitees**. Escribe el código y para ahí.

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `src/i18n/ui.ts` | El diccionario. Única fuente de verdad de todo el texto de UI. |
| `src/i18n/utils.ts` | Lógica de rutas e idioma: derivar, prefijar, despregar, emparejar. Sin texto. |
| `src/i18n/utils.test.ts` | Tests de lo anterior. |
| `src/lib/articulos.js` | Traducir entre `entry.id` de la colección y (idioma, slug). Emparejar traducciones. |
| `src/lib/articulos.test.js` | Tests de lo anterior. |
| `src/components/Header.astro` | Header fijo: nombre, link a noticias, switch de idioma. |
| `src/layouts/Layout.astro` | `<html lang>`, canonical, hreflang, script de detección. |

La división es deliberada: `utils.ts` no sabe nada de la colección de contenido y `articulos.js`
no sabe nada de URLs. Se encuentran solo en el `Map` de traducciones que uno produce y el otro
consume, y por eso los dos se pueden testear con objetos planos, sin Astro.

---

## Task 1: Diccionario y helpers de i18n

Tarea puramente aditiva: crea `src/i18n/` y no toca ningún archivo existente. Al terminar,
el sitio compila y se comporta exactamente igual que antes.

**Files:**
- Create: `src/i18n/ui.ts`
- Create: `src/i18n/utils.ts`
- Test: `src/i18n/utils.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `DEFAULT_LANG: 'es'`, `LANGS: readonly ['es','en']`, `type Lang = 'es'|'en'`
  - `ui: { es: Record<UIKey,string>, en: Record<UIKey,string> }`, `type UIKey`
  - `getLangFromUrl(pathname: string): Lang`
  - `stripLang(pathname: string): string`
  - `localizePath(lang: Lang, path: string): string`
  - `alternatePath(pathname: string, targetLang: Lang, translations: Map<string,string>): string | null`
  - `switchLangPath(pathname: string, targetLang: Lang, translations: Map<string,string>): string`
  - `useTranslations(lang: Lang): (key: UIKey, params?: Record<string,string|number>) => string`

- [ ] **Step 1: Escribir el diccionario**

Crear `src/i18n/ui.ts`:

```ts
// src/i18n/ui.ts
export const DEFAULT_LANG = 'es';
export const LANGS = ['es', 'en'] as const;

export type Lang = (typeof LANGS)[number];

// El español se declara primero y su conjunto de claves define el contrato:
// `en` se valida contra él con `satisfies`, así que una clave que falte o
// sobre en inglés es un error de compilación, no un `undefined` en producción.
const es = {
  'site.name': 'GTA 6 Countdown',

  'home.title': 'GTA 6 Countdown',
  'home.description': 'Cuenta regresiva al lanzamiento de GTA VI, noticias y comunidad.',

  'hero.title': 'Cuenta regresiva a GTA VI',
  'hero.bgAlt': 'Arte de fondo de GTA VI',
  'hero.days': 'Días',
  'hero.hours': 'Horas',
  'hero.minutes': 'Minutos',
  'hero.seconds': 'Segundos',
  'hero.disclaimer':
    'Rockstar solo confirmó la fecha de lanzamiento, no la hora exacta de desbloqueo. El contador asume medianoche hora local.',

  'waitlist.heading': 'Súmate al lanzamiento',
  'waitlist.sub': 'Marca que vas a estar el día uno. Sin email, sin registro.',
  'waitlist.countOne': 'persona ya se sumó',
  'waitlist.countOther': 'personas ya se sumaron',
  'waitlist.cta': 'Me sumo',
  'waitlist.sending': 'Sumándote…',
  'waitlist.joined': 'Ya estás dentro ✓',
  'waitlist.error': 'No pudimos sumarte. Intenta de nuevo.',

  'trailers.heading': 'Trailers',
  'trailers.watch': 'Ver',
  'trailers.aria': 'Ver {title} en YouTube',

  'news.heading': 'Noticias',
  'news.latest': 'Últimas noticias',
  'news.more': 'Ver más noticias',
  'news.title': 'Noticias — GTA 6 Countdown',
  'news.description': 'Artículos propios y últimas noticias sobre GTA VI.',

  'donations.heading': 'Ayúdanos a llegar',
  'donations.body':
    'Hola, somos un grupo de amigos que queremos juntar dinero para comprar una consola para jugar GTA VI apenas salga. Si te gusta el proyecto, cualquier aporte suma.',

  'footer.disclaimer': 'No afiliado con Rockstar Games o Take-Two Interactive.',

  'nav.home': 'Inicio',
  'nav.news': 'Noticias',
  'nav.waitlist': 'Lista de espera',
  'nav.donations': 'Donaciones',

  'music.enable': 'Activar música ambiental',
  'music.mute': 'Silenciar música ambiental',
  'music.title': 'Música ambiental',
  'music.on': 'Música encendida',
  'music.off': 'Música apagada',

  'lang.switch': 'Cambiar idioma',
} as const;

export type UIKey = keyof typeof es;

const en = {
  'site.name': 'GTA 6 Countdown',

  'home.title': 'GTA 6 Countdown',
  'home.description': 'Countdown to the GTA VI release, news and community.',

  'hero.title': 'GTA VI Countdown',
  'hero.bgAlt': 'GTA VI background art',
  'hero.days': 'Days',
  'hero.hours': 'Hours',
  'hero.minutes': 'Minutes',
  'hero.seconds': 'Seconds',
  'hero.disclaimer':
    'Rockstar only confirmed the release date, not the exact unlock time. The counter assumes midnight local time.',

  'waitlist.heading': 'Join the launch',
  'waitlist.sub': "Mark that you'll be there on day one. No email, no signup.",
  'waitlist.countOne': 'person has joined',
  'waitlist.countOther': 'people have joined',
  'waitlist.cta': "I'm in",
  'waitlist.sending': 'Joining…',
  'waitlist.joined': "You're in ✓",
  'waitlist.error': "We couldn't add you. Try again.",

  'trailers.heading': 'Trailers',
  'trailers.watch': 'Watch',
  'trailers.aria': 'Watch {title} on YouTube',

  'news.heading': 'News',
  'news.latest': 'Latest news',
  'news.more': 'See more news',
  'news.title': 'News — GTA 6 Countdown',
  'news.description': 'Our own articles and the latest GTA VI news.',

  'donations.heading': 'Help us get there',
  'donations.body':
    "Hi, we're a group of friends trying to raise money for a console so we can play GTA VI the day it drops. If you like the project, anything helps.",

  'footer.disclaimer': 'Not affiliated with Rockstar Games or Take-Two Interactive.',

  'nav.home': 'Home',
  'nav.news': 'News',
  'nav.waitlist': 'Waitlist',
  'nav.donations': 'Donations',

  'music.enable': 'Turn on ambient music',
  'music.mute': 'Mute ambient music',
  'music.title': 'Ambient music',
  'music.on': 'Music on',
  'music.off': 'Music off',

  'lang.switch': 'Change language',
} satisfies Record<UIKey, string>;

export const ui = { es, en } as const;
```

- [ ] **Step 2: Escribir los tests de rutas (fallan)**

Crear `src/i18n/utils.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import {
  alternatePath,
  getLangFromUrl,
  localizePath,
  stripLang,
  switchLangPath,
  useTranslations,
} from './utils';

// 'es:bienvenida' -> 'welcome' significa: estando en el artículo español
// "bienvenida", su contraparte en el otro idioma tiene el slug "welcome".
const TRANSLATIONS = new Map([
  ['es:bienvenida', 'welcome'],
  ['en:welcome', 'bienvenida'],
]);

describe('getLangFromUrl', () => {
  it('trata las rutas sin prefijo como el idioma por defecto', () => {
    expect(getLangFromUrl('/')).toBe('es');
    expect(getLangFromUrl('/noticias')).toBe('es');
    expect(getLangFromUrl('/noticias/bienvenida')).toBe('es');
  });

  it('reconoce el prefijo del idioma no-default', () => {
    expect(getLangFromUrl('/en/')).toBe('en');
    expect(getLangFromUrl('/en/noticias')).toBe('en');
  });

  // "english" empieza con "en" pero no es el prefijo: el match es por
  // segmento completo, no por prefijo de string.
  it('no confunde un segmento que solo empieza igual', () => {
    expect(getLangFromUrl('/english')).toBe('es');
    expect(getLangFromUrl('/entrevistas')).toBe('es');
  });
});

describe('stripLang', () => {
  it('quita el prefijo de idioma', () => {
    expect(stripLang('/en/')).toBe('/');
    expect(stripLang('/en/noticias')).toBe('/noticias');
  });

  it('deja intactas las rutas del idioma por defecto', () => {
    expect(stripLang('/')).toBe('/');
    expect(stripLang('/noticias')).toBe('/noticias');
  });
});

describe('localizePath', () => {
  it('no prefija el idioma por defecto', () => {
    expect(localizePath('es', '/')).toBe('/');
    expect(localizePath('es', '/noticias')).toBe('/noticias');
  });

  it('prefija el idioma no-default y conserva la barra en la raíz', () => {
    expect(localizePath('en', '/')).toBe('/en/');
    expect(localizePath('en', '/noticias')).toBe('/en/noticias');
  });
});

describe('alternatePath', () => {
  it('mapea las páginas fijas de un idioma al otro', () => {
    expect(alternatePath('/', 'en', TRANSLATIONS)).toBe('/en/');
    expect(alternatePath('/noticias', 'en', TRANSLATIONS)).toBe('/en/noticias');
    expect(alternatePath('/en/noticias', 'es', TRANSLATIONS)).toBe('/noticias');
  });

  it('mapea un artículo a su traducción', () => {
    expect(alternatePath('/noticias/bienvenida', 'en', TRANSLATIONS)).toBe('/en/noticias/welcome');
    expect(alternatePath('/en/noticias/welcome', 'es', TRANSLATIONS)).toBe('/noticias/bienvenida');
  });

  // Es la diferencia central con switchLangPath: el hreflang prefiere no
  // declarar nada antes que declarar una traducción que no existe.
  it('devuelve null para un artículo sin traducción', () => {
    expect(alternatePath('/noticias/otro', 'en', TRANSLATIONS)).toBeNull();
  });

  // La autorreferencia tiene que devolver la ruta tal cual. Sin el caso
  // especial, un artículo traducido devolvía el slug de su traducción.
  it('apuntar al idioma actual devuelve la ruta actual', () => {
    expect(alternatePath('/noticias/bienvenida', 'es', TRANSLATIONS)).toBe('/noticias/bienvenida');
    expect(alternatePath('/en/noticias/welcome', 'en', TRANSLATIONS)).toBe('/en/noticias/welcome');
    expect(alternatePath('/noticias/otro', 'es', TRANSLATIONS)).toBe('/noticias/otro');
  });
});

describe('switchLangPath', () => {
  it('coincide con alternatePath cuando la traducción existe', () => {
    expect(switchLangPath('/noticias/bienvenida', 'en', TRANSLATIONS)).toBe('/en/noticias/welcome');
    expect(switchLangPath('/', 'en', TRANSLATIONS)).toBe('/en/');
  });

  // El switch prioriza que el usuario no choque con un 404: cae al índice
  // de noticias del idioma destino en vez de devolver nada.
  it('cae al índice de noticias cuando el artículo no está traducido', () => {
    expect(switchLangPath('/noticias/otro', 'en', TRANSLATIONS)).toBe('/en/noticias');
    expect(switchLangPath('/en/noticias/other', 'es', TRANSLATIONS)).toBe('/noticias');
  });

  // El chip activo del switch usa esta misma función, así que tiene que
  // quedarse donde está en vez de saltar al slug de la traducción.
  it('apuntar al idioma actual devuelve la misma ruta', () => {
    expect(switchLangPath('/noticias', 'es', TRANSLATIONS)).toBe('/noticias');
    expect(switchLangPath('/noticias/bienvenida', 'es', TRANSLATIONS)).toBe('/noticias/bienvenida');
  });
});

describe('useTranslations', () => {
  it('devuelve el texto del idioma pedido', () => {
    expect(useTranslations('es')('nav.news')).toBe('Noticias');
    expect(useTranslations('en')('nav.news')).toBe('News');
  });

  it('interpola parámetros', () => {
    expect(useTranslations('en')('trailers.aria', { title: 'Trailer 2' })).toBe(
      'Watch Trailer 2 on YouTube',
    );
  });
});
```

- [ ] **Step 3: Verificar que fallan**

Comando (lo corre el dueño del repo): `pnpm vitest run src/i18n/utils.test.ts`
Esperado: FAIL — no existe `./utils`.

- [ ] **Step 4: Implementar los helpers**

Crear `src/i18n/utils.ts`:

```ts
// src/i18n/utils.ts
import { DEFAULT_LANG, LANGS, ui, type Lang, type UIKey } from './ui';

// Solo los idiomas distintos del default llevan prefijo en la URL.
const PREFIXED: readonly string[] = LANGS.filter((lang) => lang !== DEFAULT_LANG);

// Un artículo es exactamente /noticias/<slug>, un solo segmento. La barra
// final es opcional porque el build estático puede servir ambas formas.
const ARTICLE_PATH = /^\/noticias\/([^/]+)\/?$/;

// El idioma sale del primer segmento completo de la ruta. Comparar segmentos
// y no prefijos de string evita que "/english" se lea como inglés.
export function getLangFromUrl(pathname: string): Lang {
  const segment = pathname.split('/')[1];
  return PREFIXED.includes(segment) ? (segment as Lang) : DEFAULT_LANG;
}

export function stripLang(pathname: string): string {
  const lang = getLangFromUrl(pathname);
  if (lang === DEFAULT_LANG) return pathname;
  const rest = pathname.slice(lang.length + 1);
  return rest === '' ? '/' : rest;
}

export function localizePath(lang: Lang, path: string): string {
  const clean = path.startsWith('/') ? path : `/${path}`;
  if (lang === DEFAULT_LANG) return clean;
  // La raíz conserva la barra final: /en/ y no /en, que Vercel redirigiría.
  return clean === '/' ? `/${lang}/` : `/${lang}${clean}`;
}

// La contraparte real de esta ruta en el otro idioma, o null si no existe.
// Es lo que consume el hreflang, que no puede declarar traducciones falsas.
export function alternatePath(
  pathname: string,
  targetLang: Lang,
  translations: Map<string, string>,
): string | null {
  const currentLang = getLangFromUrl(pathname);
  const bare = stripLang(pathname);

  // Apuntar al idioma actual es la autorreferencia: el hreflang la necesita y
  // el switch la usa para el chip activo. Sin esta salida temprana, un
  // artículo traducido devolvería el slug de SU TRADUCCIÓN como si fuera el
  // propio (/noticias/welcome estando en /noticias/bienvenida), que es un 404.
  if (targetLang === currentLang) return localizePath(currentLang, bare);

  const article = bare.match(ARTICLE_PATH);

  if (article) {
    const counterpart = translations.get(`${currentLang}:${article[1]}`);
    return counterpart ? localizePath(targetLang, `/noticias/${counterpart}`) : null;
  }

  return localizePath(targetLang, bare);
}

// Lo que consume el switch. A diferencia del hreflang, siempre tiene que dar
// un destino navegable: un artículo sin traducir manda al índice de noticias
// del otro idioma en vez de a un 404.
export function switchLangPath(
  pathname: string,
  targetLang: Lang,
  translations: Map<string, string>,
): string {
  return alternatePath(pathname, targetLang, translations) ?? localizePath(targetLang, '/noticias');
}

export function useTranslations(lang: Lang) {
  return function t(key: UIKey, params?: Record<string, string | number>): string {
    let value: string = ui[lang][key];
    if (params) {
      for (const [name, replacement] of Object.entries(params)) {
        value = value.replaceAll(`{${name}}`, String(replacement));
      }
    }
    return value;
  };
}
```

- [ ] **Step 5: Checkpoint**

Verificación sugerida al dueño del repo:

```bash
pnpm vitest run src/i18n/utils.test.ts   # todos en verde
pnpm exec astro check                    # sin errores de tipos
```

Commit sugerido: `feat(i18n): agregar diccionario es/en y helpers de rutas`

---

## Task 2: Colección de artículos bilingüe

Reestructura el contenido y agrega los helpers que traducen entre `entry.id` y
(idioma, slug). Las tres páginas existentes se ajustan lo mínimo para que **las URLs
del sitio no cambien**: sigue siendo un sitio monolingüe, pero la colección ya tiene
las dos versiones. El artículo en inglés existe y no se renderiza todavía.

**Files:**
- Create: `src/lib/articulos.js`
- Test: `src/lib/articulos.test.js`
- Move: `src/content/articulos/bienvenida.md` → `src/content/articulos/es/bienvenida.md`
- Move: `src/content/articulos/portada.jpg` → `src/content/articulos/_assets/portada.jpg`
- Create: `src/content/articulos/en/welcome.md`
- Modify: `src/content.config.ts`
- Modify: `src/pages/index.astro`, `src/pages/noticias/index.astro`, `src/pages/noticias/[slug].astro`

**Interfaces:**
- Consumes: nada de la Task 1.
- Produces:
  - `articleLang(id: string): 'es'|'en'` — lanza si el id no está en un directorio de idioma
  - `articleSlug(id: string): string` — lanza si el artículo está anidado más allá del directorio de idioma
  - `byLang(entries, lang): entries`
  - `buildTranslationIndex(entries): Map<string,string>` — claves `` `${lang}:${slug}` ``, valor: el slug de la contraparte. Es exactamente el `Map` que consumen `alternatePath` y `switchLangPath` de la Task 1.

- [ ] **Step 1: Escribir los tests (fallan)**

Crear `src/lib/articulos.test.js`:

```js
import { describe, expect, it } from 'vitest';
import { articleLang, articleSlug, byLang, buildTranslationIndex } from './articulos.js';

// Forma mínima de una entrada de colección: solo id y data. Los helpers no
// necesitan Astro, así que se testean con objetos planos.
const entry = (id, data = {}) => ({ id, data });

describe('articleLang', () => {
  it('deriva el idioma del directorio', () => {
    expect(articleLang('es/bienvenida')).toBe('es');
    expect(articleLang('en/welcome')).toBe('en');
  });

  // Un artículo mal ubicado desaparecería del sitio sin avisar, que es peor
  // fallo que un build roto. Por eso lanza en vez de devolver null.
  it('lanza si el artículo no está en un directorio de idioma', () => {
    expect(() => articleLang('bienvenida')).toThrow(/directorio de idioma/);
    expect(() => articleLang('borradores/algo')).toThrow(/directorio de idioma/);
  });
});

describe('articleSlug', () => {
  it('quita el directorio de idioma', () => {
    expect(articleSlug('es/bienvenida')).toBe('bienvenida');
    expect(articleSlug('en/welcome')).toBe('welcome');
  });

  // El slug es un solo segmento: la ruta es [slug].astro y ARTICLE_PATH en
  // i18n/utils.ts también asume un segmento. Un .md anidado más hondo
  // generaría una URL que ninguna ruta atiende.
  it('lanza si el artículo está anidado más allá del directorio de idioma', () => {
    expect(() => articleSlug('es/2026/analisis')).toThrow(/un solo segmento/);
  });
});

describe('byLang', () => {
  it('filtra las entradas del idioma pedido', () => {
    const entries = [entry('es/bienvenida'), entry('en/welcome'), entry('es/otro')];
    expect(byLang(entries, 'es').map((e) => e.id)).toEqual(['es/bienvenida', 'es/otro']);
    expect(byLang(entries, 'en').map((e) => e.id)).toEqual(['en/welcome']);
  });
});

describe('buildTranslationIndex', () => {
  it('empareja por translationKey explícito', () => {
    const index = buildTranslationIndex([
      entry('es/bienvenida'),
      entry('en/welcome', { translationKey: 'bienvenida' }),
    ]);
    expect(index.get('es:bienvenida')).toBe('welcome');
    expect(index.get('en:welcome')).toBe('bienvenida');
  });

  // El default al propio slug es lo que permite no escribir nada en el
  // frontmatter cuando el slug es igual en los dos idiomas.
  it('empareja por slug igual cuando no hay translationKey', () => {
    const index = buildTranslationIndex([entry('es/trailer-2'), entry('en/trailer-2')]);
    expect(index.get('es:trailer-2')).toBe('trailer-2');
    expect(index.get('en:trailer-2')).toBe('trailer-2');
  });

  it('no registra nada para un artículo sin contraparte', () => {
    const index = buildTranslationIndex([entry('es/solo-en-espanol')]);
    expect(index.get('es:solo-en-espanol')).toBeUndefined();
    expect(index.size).toBe(0);
  });
});
```

- [ ] **Step 2: Verificar que fallan**

Comando: `pnpm vitest run src/lib/articulos.test.js`
Esperado: FAIL — no existe `./articulos.js`.

- [ ] **Step 3: Implementar los helpers**

Crear `src/lib/articulos.js`:

```js
// src/lib/articulos.js
//
// El idioma de un artículo sale del directorio en el que vive, no de un campo
// del frontmatter: un `lang: en` dentro de es/ puede mentir, la ruta no.
export const LOCALE_DIRS = ['es', 'en'];

export function articleLang(id) {
  const dir = id.split('/')[0];
  if (!LOCALE_DIRS.includes(dir)) {
    throw new Error(
      `El artículo "${id}" no está dentro de un directorio de idioma. ` +
        `Muévelo a src/content/articulos/<${LOCALE_DIRS.join('|')}>/.`
    );
  }
  return dir;
}

export function articleSlug(id) {
  articleLang(id);
  const rest = id.split('/').slice(1);
  if (rest.length !== 1) {
    throw new Error(
      `El artículo "${id}" tiene que ser un solo segmento dentro del directorio ` +
        `de idioma: la ruta es [slug].astro y no atiende subdirectorios.`
    );
  }
  return rest[0];
}

export function byLang(entries, lang) {
  return entries.filter((entry) => articleLang(entry.id) === lang);
}

// Devuelve un Map de `${lang}:${slug}` al slug de la contraparte. Solo entran
// los artículos que tienen traducción; los que no, quedan fuera a propósito,
// porque es lo que deja al hreflang no declarar traducciones inexistentes.
export function buildTranslationIndex(entries) {
  const groups = new Map();

  for (const entry of entries) {
    const lang = articleLang(entry.id);
    const slug = articleSlug(entry.id);
    // El default al propio slug hace que dos archivos con el mismo nombre se
    // emparejen sin escribir nada en el frontmatter.
    const key = entry.data.translationKey ?? slug;
    if (!groups.has(key)) groups.set(key, new Map());
    groups.get(key).set(lang, slug);
  }

  const index = new Map();
  for (const langs of groups.values()) {
    for (const [lang, slug] of langs) {
      for (const [otherLang, otherSlug] of langs) {
        if (otherLang !== lang) index.set(`${lang}:${slug}`, otherSlug);
      }
    }
  }
  return index;
}
```

- [ ] **Step 4: Mover el contenido**

```bash
mkdir -p src/content/articulos/es src/content/articulos/en src/content/articulos/_assets
git mv src/content/articulos/bienvenida.md src/content/articulos/es/bienvenida.md
git mv src/content/articulos/portada.jpg src/content/articulos/_assets/portada.jpg
```

Editar `src/content/articulos/es/bienvenida.md` para reapuntar la portada. Solo cambia
esa línea del frontmatter:

```yaml
cover: "../_assets/portada.jpg"
```

- [ ] **Step 5: Escribir el artículo en inglés**

Crear `src/content/articulos/en/welcome.md`:

```markdown
---
title: "Welcome to the GTA 6 countdown"
description: "Why we built this site and what you'll find here."
pubDate: 2026-08-29
author: "The team"
cover: "../_assets/portada.jpg"
coverAlt: "GTA VI cover art"
translationKey: bienvenida
---

We built this site to have a place of our own to follow the countdown to GTA VI,
share news and write about what the game means to us. Over the coming weeks we'll
be adding analysis and opinion pieces.
```

- [ ] **Step 6: Agregar `translationKey` al schema**

Modificar `src/content.config.ts`. Dentro del objeto `z.object({ … })`, después de
`coverAlt`, agregar:

```ts
      // Empareja un artículo con su traducción. Opcional: si se omite, el
      // emparejamiento cae al propio slug, que basta cuando el nombre del
      // archivo es igual en los dos idiomas.
      translationKey: z.string().optional(),
```

- [ ] **Step 7: Ajustar las tres páginas para que las URLs no cambien**

En `src/pages/index.astro`, cambiar la línea que carga los artículos:

```astro
const articulos = await getCollection('articulos');
```

por:

```astro
const articulos = byLang(await getCollection('articulos'), 'es');
```

y agregar el import:

```astro
import { byLang } from '../lib/articulos.js';
```

En `src/pages/noticias/index.astro`, el mismo cambio con el import
`from '../../lib/articulos.js'`.

En `src/pages/noticias/[slug].astro`, reemplazar `getStaticPaths` completo:

```astro
export async function getStaticPaths() {
	const articulos = byLang(await getCollection('articulos'), 'es');
	return articulos.map((entry) => ({
		params: { slug: articleSlug(entry.id) },
		props: { entry },
	}));
}
```

y agregar el import:

```astro
import { articleSlug, byLang } from '../../lib/articulos.js';
```

En `src/pages/index.astro` y `src/pages/noticias/index.astro`, los `href` de las tarjetas
usan `entry.entry.id`, que ahora vale `es/bienvenida`. Reemplazar en **ambos** archivos
todas las apariciones de:

```astro
href={`/noticias/${entry.entry.id}`}
```

por:

```astro
href={`/noticias/${articleSlug(entry.entry.id)}`}
```

(`noticias/index.astro` tiene **dos** apariciones: una en la grilla visible y otra en la
grilla `#news-grid-rest`.)

- [ ] **Step 8: Checkpoint**

Verificación sugerida:

```bash
pnpm vitest run                # articulos.test.js y los existentes en verde
pnpm exec astro build          # sin errores
ls dist/client/noticias/       # bienvenida/ presente; welcome/ NO (aún no se renderiza)
```

Commit sugerido: `refactor(contenido): separar artículos por idioma en es/ y en/`

---

## Task 3: Rutas bilingües

Mueve las páginas a `[...locale]/` y genera las dos versiones. Al terminar existen
`/en/`, `/en/noticias` y `/en/noticias/welcome`. El texto de las **páginas** ya sale
del diccionario; el de los **componentes** todavía está en español (Task 6 y 7).

**Files:**
- Modify: `astro.config.mjs`
- Move: `src/pages/index.astro` → `src/pages/[...locale]/index.astro`
- Move: `src/pages/noticias/index.astro` → `src/pages/[...locale]/noticias/index.astro`
- Move: `src/pages/noticias/[slug].astro` → `src/pages/[...locale]/noticias/[slug].astro`

**Interfaces:**
- Consumes: `getLangFromUrl`, `useTranslations`, `localizePath` (Task 1); `articleLang`, `articleSlug`, `byLang` (Task 2).
- Produces: las rutas `/`, `/en/`, `/noticias`, `/en/noticias`, `/noticias/:slug`, `/en/noticias/:slug`.

- [ ] **Step 1: Configurar `site` e `i18n`**

Modificar `astro.config.mjs`. Dentro de `defineConfig({ … })`, junto a `adapter`:

```js
  site: 'https://countdowngtavi.com',

  // Declara los locales ante el framework. La generación de rutas es manual,
  // vía el rest param [...locale] de src/pages, y el idioma se deriva con
  // getLangFromUrl: un solo camino de código, cubierto por tests, sin depender
  // de cómo interactúa la i18n nativa con rutas de rest param.
  i18n: {
    defaultLocale: 'es',
    locales: ['es', 'en'],
    routing: { prefixDefaultLocale: false },
  },
```

- [ ] **Step 2: Mover las páginas**

```bash
mkdir -p 'src/pages/[...locale]/noticias'
git mv src/pages/index.astro 'src/pages/[...locale]/index.astro'
git mv src/pages/noticias/index.astro 'src/pages/[...locale]/noticias/index.astro'
git mv 'src/pages/noticias/[slug].astro' 'src/pages/[...locale]/noticias/[slug].astro'
rmdir src/pages/noticias
```

Todos los imports relativos de las tres páginas bajan un nivel: `../` pasa a `../../`
en `[...locale]/index.astro`, y `../../` pasa a `../../../` en las dos de `noticias/`.

- [ ] **Step 3: Reescribir el frontmatter de la home**

En `src/pages/[...locale]/index.astro`, reemplazar el bloque de frontmatter entero por:

```astro
---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import Hero from '../../components/Hero.astro';
import Waitlist from '../../components/Waitlist.astro';
import NewsCard from '../../components/NewsCard.astro';
import Donations from '../../components/Donations.astro';
import TrailerCard from '../../components/TrailerCard.astro';
import { fetchAggregated, mergeNews } from '../../lib/news.js';
import { getLatestTrailers, watchUrl } from '../../lib/trailers.js';
import { articleSlug, byLang } from '../../lib/articulos.js';
import { getLangFromUrl, localizePath, useTranslations } from '../../i18n/utils';
import { DEFAULT_LANG, LANGS } from '../../i18n/ui';

// locale: undefined genera la ruta sin prefijo (/), que es cómo el idioma
// por defecto se queda en las URLs que el sitio ya tenía indexadas.
export function getStaticPaths() {
	return LANGS.map((lang) => ({
		params: { locale: lang === DEFAULT_LANG ? undefined : lang },
	}));
}

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);

const trailers = getLatestTrailers(2);
const aggregated = await fetchAggregated();
const articulos = byLang(await getCollection('articulos'), lang);
const merged = mergeNews(articulos, aggregated);

const PREVIEW_COUNT = 9;

// The team's own articles are the site's differentiator, so always reserve a
// preview slot for the newest one instead of leaving it to open date
// competition with high-volume aggregated feeds.
const newestPropio = merged.find((entry) => entry.kind === 'propio');
const preview = newestPropio
	? [
			newestPropio,
			...merged.filter((entry) => entry !== newestPropio).slice(0, PREVIEW_COUNT - 1),
		].sort((a, b) => b.date.getTime() - a.date.getTime())
	: merged.slice(0, PREVIEW_COUNT);
---
```

- [ ] **Step 4: Traducir el marcado de la home**

En el mismo archivo, cambiar estas cinco cosas del template:

```astro
<Layout title={t('home.title')} description={t('home.description')}>
```

```astro
		<h2 class="text-2xl font-black uppercase mb-8">{t('trailers.heading')}</h2>
```

```astro
		<h2 class="text-2xl font-black uppercase mb-8">{t('news.latest')}</h2>
```

El `href` de la tarjeta propia (dentro del `preview.map`):

```astro
						href={localizePath(lang, `/noticias/${articleSlug(entry.entry.id)}`)}
```

Y el link del final de la sección de noticias:

```astro
		<a href={localizePath(lang, '/noticias')} class="mt-4 mx-auto block w-fit rounded-full border border-white/20 px-6 py-2 text-sm uppercase tracking-wide hover:bg-white/10">
			{t('news.more')}
		</a>
```

El bloque `<script>` de GSAP al final del archivo no cambia.

- [ ] **Step 5: Reescribir el índice de noticias**

En `src/pages/[...locale]/noticias/index.astro`, reemplazar el frontmatter por:

```astro
---
import { getCollection } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';
import NewsCard from '../../../components/NewsCard.astro';
import { fetchAggregated, mergeNews } from '../../../lib/news.js';
import { articleSlug, byLang } from '../../../lib/articulos.js';
import { getLangFromUrl, localizePath, useTranslations } from '../../../i18n/utils';
import { DEFAULT_LANG, LANGS } from '../../../i18n/ui';

export function getStaticPaths() {
	return LANGS.map((lang) => ({
		params: { locale: lang === DEFAULT_LANG ? undefined : lang },
	}));
}

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);

const aggregated = await fetchAggregated();
const articulos = byLang(await getCollection('articulos'), lang);
const merged = mergeNews(articulos, aggregated);

const INITIAL_COUNT = 9;
const visible = merged.slice(0, INITIAL_COUNT);
const rest = merged.slice(INITIAL_COUNT);
---
```

En el template del mismo archivo:

```astro
<Layout title={t('news.title')} description={t('news.description')}>
	<main class="mx-auto max-w-5xl px-4 pt-24 pb-16">
		<h1 class="text-3xl font-black uppercase mb-8">{t('news.heading')}</h1>
```

El `pt-24` reemplaza al `py-16` para despejar el header fijo de la Task 5.

Los **dos** `href` de tarjeta propia (grilla visible y `#news-grid-rest`):

```astro
						href={localizePath(lang, `/noticias/${articleSlug(entry.entry.id)}`)}
```

Y el botón:

```astro
				<button id="load-more" class="mt-4 mx-auto block rounded-full border border-white/20 px-6 py-2 text-sm uppercase tracking-wide hover:bg-white/10">
					{t('news.more')}
				</button>
```

El `<script>` del final no cambia.

- [ ] **Step 6: Reescribir la página de artículo**

`src/pages/[...locale]/noticias/[slug].astro` completo:

```astro
---
// src/pages/[...locale]/noticias/[slug].astro
import { getCollection, render } from 'astro:content';
import { Image } from 'astro:assets';
import Layout from '../../../layouts/Layout.astro';
import { articleLang, articleSlug } from '../../../lib/articulos.js';
import { getLangFromUrl, useTranslations } from '../../../i18n/utils';
import { DEFAULT_LANG } from '../../../i18n/ui';

// Cada artículo pertenece a exactamente un idioma, así que el locale de cada
// ruta sale del directorio en el que vive el archivo.
export async function getStaticPaths() {
	const articulos = await getCollection('articulos');
	return articulos.map((entry) => {
		const lang = articleLang(entry.id);
		return {
			params: {
				locale: lang === DEFAULT_LANG ? undefined : lang,
				slug: articleSlug(entry.id),
			},
			props: { entry },
		};
	});
}

const { entry } = Astro.props;
const { Content } = await render(entry);

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);
---

<Layout title={`${entry.data.title} — ${t('site.name')}`} description={entry.data.description}>
	<article class="mx-auto max-w-3xl px-4 pt-24 pb-16">
		<p class="text-xs uppercase tracking-wide text-white/50">
			{entry.data.author} · {entry.data.pubDate.toLocaleDateString(lang, { day: 'numeric', month: 'long', year: 'numeric', timeZone: 'UTC' })}
		</p>
		<h1 class="mt-2 text-3xl font-black">{entry.data.title}</h1>
		<Image src={entry.data.cover} alt={entry.data.coverAlt} class="mt-6 w-full rounded-lg object-cover" widths={[600, 1200]} sizes="(min-width: 768px) 768px, 100vw" />
		<div class="prose prose-invert mt-8 max-w-none">
			<Content />
		</div>
	</article>
</Layout>
```

- [ ] **Step 7: Checkpoint**

Verificación sugerida:

```bash
pnpm exec astro build
ls dist/client/                     # index.html, en/, noticias/
ls dist/client/en/noticias/         # welcome/ presente
ls dist/client/noticias/            # bienvenida/ presente
```

Commit sugerido: `feat(i18n): generar rutas en español e inglés con [...locale]`

---

## Task 4: Layout — idioma, SEO y detección

**Files:**
- Modify: `src/layouts/Layout.astro`

**Interfaces:**
- Consumes: `getLangFromUrl`, `alternatePath` (Task 1); `buildTranslationIndex` (Task 2).
- Produces: `<html lang>` correcto, `canonical`, `hreflang`, y el redirect de detección.

- [ ] **Step 1: Reescribir el frontmatter del Layout**

Reemplazar el bloque de frontmatter de `src/layouts/Layout.astro` por:

```astro
---
// src/layouts/Layout.astro
import "../styles/global.css";
import { getCollection } from "astro:content";
import Footer from "../components/Footer.astro";
import MusicToggle from "../components/MusicToggle.astro";
import Analytics from "@vercel/analytics/astro";
import { buildTranslationIndex } from "../lib/articulos.js";
import { alternatePath, getLangFromUrl } from "../i18n/utils";
import { DEFAULT_LANG, LANGS } from "../i18n/ui";

interface Props {
    title: string;
    description: string;
}

const { title, description } = Astro.props;

const pathname = Astro.url.pathname;
const lang = getLangFromUrl(pathname);

// El 404 responde a cualquier URL inexistente, así que no tiene una URL
// canónica propia ni una contraparte que declarar.
const isNotFound = pathname === "/404" || pathname === "/404/";

const translations = buildTranslationIndex(await getCollection("articulos"));

const canonical = isNotFound ? null : new URL(pathname, Astro.site).href;

// Solo se declara un hreflang cuando la contraparte existe de verdad. Un
// artículo sin traducir no emite ninguno: apuntar al índice, como sí hace el
// switch, sería declarar una traducción falsa y Google lo descarta.
const alternates = isNotFound
    ? []
    : LANGS.map((code) => ({ code, path: alternatePath(pathname, code, translations) }))
          .filter((entry) => entry.path !== null)
          .map((entry) => ({ code: entry.code, href: new URL(entry.path!, Astro.site).href }));

const xDefault = alternates.find((entry) => entry.code === DEFAULT_LANG)?.href ?? null;

// La detección solo corre en la raíz en español: entrar directo a cualquier
// otra URL es un link explícito y se respeta.
const detectLanguage = pathname === "/";
---
```

- [ ] **Step 2: Reescribir el `<head>` y el `<html>`**

Reemplazar desde `<!doctype html>` hasta el cierre de `</head>` por:

```astro
<!doctype html>
<html lang={lang}>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        {
            /* Bloqueante y antes de cualquier estilo: corre antes del primer
               paint, así que el visitante nunca llega a ver la home en español
               antes de rebotar. `location.replace` no ensucia el historial, y
               no se escribe nada en localStorage: solo el switch escribe, lo
               que deja la detección idempotente. */
            detectLanguage && (
                <script is:inline>
                    {`try {
  if (!localStorage.getItem('gta6:lang') && navigator.language.toLowerCase().startsWith('en')) {
    location.replace('/en/');
  }
} catch (e) {}`}
                </script>
            )
        }
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="icon" href="/favicon.ico" />
        <meta name="generator" content={Astro.generator} />
        <meta name="description" content={description} />
        {canonical && <link rel="canonical" href={canonical} />}
        {alternates.map((entry) => <link rel="alternate" hreflang={entry.code} href={entry.href} />)}
        {xDefault && <link rel="alternate" hreflang="x-default" href={xDefault} />}
        <title>{title}</title>
        <script
            async
            src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3667957730963677"
            crossorigin="anonymous"></script>
    </head>
```

El `<body>` no cambia en esta tarea.

- [ ] **Step 3: Checkpoint**

Verificación sugerida:

```bash
pnpm exec astro build
grep -o '<html lang="[a-z]*"' dist/client/en/index.html      # lang="en"
grep -o 'hreflang="[a-z-]*"' dist/client/index.html          # es, en, x-default
grep -c 'hreflang' dist/client/404.html                      # 0
grep -o "location.replace" dist/client/index.html            # presente
grep -c "location.replace" dist/client/noticias/index.html   # 0
```

Commit sugerido: `feat(i18n): lang dinámico, hreflang, canonical y detección en la raíz`

---

## Task 5: Header y switch de idioma

**Files:**
- Create: `src/components/Header.astro`
- Modify: `src/layouts/Layout.astro` (solo el `<body>`)
- Modify: `src/pages/404.astro` (padding)

**Interfaces:**
- Consumes: `getLangFromUrl`, `useTranslations`, `localizePath`, `switchLangPath` (Task 1); `buildTranslationIndex` (Task 2).
- Produces: `<Header />`, sin props.

- [ ] **Step 1: Crear el Header**

Crear `src/components/Header.astro`:

```astro
---
// src/components/Header.astro
//
// Sin props a propósito: todo se deriva de la URL y de la colección. El Header
// lee los pares de traducción él mismo en vez de recibirlos como prop, para no
// arrastrarlos por el Layout — es costo de build y la colección ya está cacheada.
import { getCollection } from 'astro:content';
import { buildTranslationIndex } from '../lib/articulos.js';
import { getLangFromUrl, localizePath, switchLangPath, useTranslations } from '../i18n/utils';
import { LANGS } from '../i18n/ui';

const pathname = Astro.url.pathname;
const lang = getLangFromUrl(pathname);
const t = useTranslations(lang);
const translations = buildTranslationIndex(await getCollection('articulos'));
---

<header
	id="site-header"
	class="fixed inset-x-0 top-0 z-40 border-b border-transparent transition-colors duration-300"
>
	<nav class="mx-auto flex max-w-5xl items-center gap-4 px-4 py-3">
		<a
			href={localizePath(lang, '/')}
			class="gta-outline-soft text-sm font-black uppercase tracking-wide hover:text-white"
		>
			{t('site.name')}
		</a>

		<a
			href={localizePath(lang, '/noticias')}
			class="ml-auto text-sm uppercase tracking-wide text-white/70 transition-colors hover:text-white"
		>
			{t('nav.news')}
		</a>

		{/* Son <a> reales, no botones: funcionan sin JS, Google los rastrea, y su
		    href es el mismo valor que calcula el hreflang del Layout. */}
		<div
			class="flex overflow-hidden rounded-full border border-white/20 bg-black/70 text-xs font-bold uppercase backdrop-blur"
			role="group"
			aria-label={t('lang.switch')}
		>
			{
				LANGS.map((code) => (
					<a
						href={switchLangPath(pathname, code, translations)}
						hreflang={code}
						lang={code}
						data-lang={code}
						aria-current={code === lang ? 'true' : undefined}
						class="px-3 py-1 transition-colors hover:bg-white/10 aria-[current=true]:bg-white aria-[current=true]:text-black"
					>
						{code}
					</a>
				))
			}
		</div>
	</nav>
</header>

<script>
	import { gsap } from 'gsap';
	import { ScrollTrigger } from 'gsap/ScrollTrigger';

	gsap.registerPlugin(ScrollTrigger);

	const header = document.getElementById('site-header');

	if (header) {
		// Transparente arriba del todo y sólido al bajar. En la home eso descubre
		// el hero completo; en el resto del sitio el body ya es negro, así que el
		// estado transparente es indistinguible del sólido y no hace falta que el
		// componente sepa en qué página está.
		const SOLID = ['bg-black/70', 'backdrop-blur', 'border-white/10'];

		ScrollTrigger.create({
			trigger: document.body,
			start: 'top -80',
			onEnter: () => header.classList.add(...SOLID),
			onLeaveBack: () => header.classList.remove(...SOLID),
		});

		// La preferencia se guarda antes de que la navegación siga su curso. Sin
		// JS el link funciona igual, solo no se recuerda. El try/catch es el mismo
		// que usan MusicToggle y Waitlist: el modo privado puede lanzar.
		for (const link of header.querySelectorAll<HTMLAnchorElement>('[data-lang]')) {
			link.addEventListener('click', () => {
				try {
					localStorage.setItem('gta6:lang', link.dataset.lang!);
				} catch {
					// El switch igual navega; solo no queda recordado.
				}
			});
		}
	}
</script>
```

- [ ] **Step 2: Montar el Header en el Layout**

En `src/layouts/Layout.astro`, agregar el import junto a los otros componentes:

```astro
import Header from "../components/Header.astro";
```

y reemplazar el `<body>` por:

```astro
    <body class="bg-black text-white">
        <Header />
        <slot />
        <Footer />
        <MusicToggle />
        <Analytics />
    </body>
```

- [ ] **Step 3: Despejar el header en el 404**

En `src/pages/404.astro`, cambiar la clase de la `<section>`:

```astro
	<section class="mx-auto max-w-2xl px-4 pt-28 pb-20 text-center">
```

- [ ] **Step 4: Checkpoint**

Verificación sugerida:

```bash
pnpm exec astro dev --background
```

Comprobar a mano en el navegador:
- En `/` el header se ve transparente sobre el hero y se vuelve sólido al bajar.
- El switch en `/noticias/bienvenida` lleva a `/en/noticias/welcome`.
- Después de usarlo, recargar `/` con el navegador en inglés **no** redirige.
- Borrar `gta6:lang` de localStorage y recargar `/`: redirige a `/en/`.

Commit sugerido: `feat(i18n): agregar header fijo con switch de idioma`

---

## Task 6: Traducir los componentes sin estado en cliente

**Files:**
- Modify: `src/components/Hero.astro`, `src/components/Donations.astro`, `src/components/Footer.astro`, `src/components/TrailerCard.astro`, `src/components/NewsCard.astro`
- Modify: `src/pages/[...locale]/index.astro` (pasar `lang` a `NewsCard` y `TrailerCard`)

**Interfaces:**
- Consumes: `getLangFromUrl`, `useTranslations`, `localizePath` (Task 1).
- Produces: `NewsCard` y `TrailerCard` ganan una prop `lang: Lang`.

- [ ] **Step 1: Hero**

En `src/components/Hero.astro`, agregar al frontmatter:

```astro
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);
```

Y en el marcado, reemplazar los seis textos:

```astro
		alt={t('hero.bgAlt')}
```
```astro
		<h1 class="gta-outline text-4xl sm:text-6xl md:text-7xl font-black uppercase tracking-wide">
			{t('hero.title')}
		</h1>
```

En los cuatro bloques del contador, cada `<span>` de etiqueta:

```astro
				<span class="gta-outline-soft text-xs sm:text-sm uppercase tracking-widest text-white/80">{t('hero.days')}</span>
```

(igual con `hero.hours`, `hero.minutes`, `hero.seconds` en su bloque correspondiente).

Y el párrafo del final:

```astro
		<p class="max-w-md text-xs sm:text-sm text-white/80">
			{t('hero.disclaimer')}
		</p>
```

El `<script>` del Hero no cambia: solo maneja números, no texto.

- [ ] **Step 2: Donations**

`src/components/Donations.astro` completo:

```astro
---
// src/components/Donations.astro
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);

const paypalUser = 'countdowngtavi';
---

<section id="donaciones" class="mx-auto max-w-2xl px-4 py-20 text-center">
	<h2 class="text-2xl font-black uppercase">{t('donations.heading')}</h2>
	<p class="mt-4 text-white/70">{t('donations.body')}</p>
	<div class="mt-8 flex justify-center">
		<a href={`https://www.paypal.me/${paypalUser}`} target="_blank" rel="noopener noreferrer">
			{t('donations.cta')}
		</a>
	</div>
</section>
```

- [ ] **Step 3: Footer**

`src/components/Footer.astro` completo:

```astro
---
// src/components/Footer.astro
import { getLangFromUrl, localizePath, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);
const year = new Date().getFullYear();
---

<footer class="border-t border-white/10 px-4 py-8 text-center text-sm text-white/50">
	<p>© {year} {t('site.name')}. {t('footer.disclaimer')}</p>
	<p class="mt-2">
		<a href={localizePath(lang, '/')} class="underline hover:text-white">{t('nav.home')}</a>
		<span class="mx-2">·</span>
		<a href={localizePath(lang, '/noticias')} class="underline hover:text-white">{t('nav.news')}</a>
		<span class="mx-2">·</span>
		<a href={`${localizePath(lang, '/')}#waitlist`} class="underline hover:text-white">{t('nav.waitlist')}</a>
		<span class="mx-2">·</span>
		<a href="#donaciones" class="underline hover:text-white">{t('nav.donations')}</a>
	</p>
</footer>
```

- [ ] **Step 4: TrailerCard**

En `src/components/TrailerCard.astro`, agregar `lang` a la interfaz de props y usar el diccionario:

```astro
---
// src/components/TrailerCard.astro
import TrailerPoster from './TrailerPoster.astro';
import { useTranslations } from '../i18n/utils';
import type { Lang } from '../i18n/ui';

interface Props {
	href: string;
	label: string;
	title: string;
	poster: 'sunset' | 'neon';
	uid: string;
	lang: Lang;
}

const { href, label, title, poster, uid, lang } = Astro.props;
const t = useTranslations(lang);
---
```

En el marcado, el `aria-label` del `<a>`:

```astro
	aria-label={t('trailers.aria', { title })}
```

y el rótulo pequeño de abajo:

```astro
		<p class="text-xs font-bold uppercase tracking-[0.35em] text-white/80">{t('trailers.watch')}</p>
```

El `{label}` que va debajo **no** cambia: es el nombre oficial del trailer.

- [ ] **Step 5: NewsCard**

En `src/components/NewsCard.astro`, agregar `lang` a las dos interfaces y al formateo.

En `PropioProps` y en `AgregadoProps`, agregar la misma línea:

```ts
	lang: Lang;
```

Agregar el import de tipo arriba del todo del frontmatter:

```astro
import type { Lang } from '../i18n/ui';
```

Y reemplazar `formatDate` por una versión que reciba el idioma:

```astro
function formatDate(date: Date | null, lang: Lang, dateOnly = false) {
	if (!date) return '';
	return date.toLocaleDateString(lang, {
		day: 'numeric',
		month: 'short',
		year: 'numeric',
		...(dateOnly ? { timeZone: 'UTC' } : {}),
	});
}
```

En el marcado hay **dos** llamadas que actualizar:

```astro
				<p class="text-xs uppercase tracking-wide text-white/50">{formatDate(props.pubDate, props.lang, true)}</p>
```

```astro
				<p class="text-xs uppercase tracking-wide text-white/50">
					{props.source} · {formatDate(props.pubDate, props.lang)}
				</p>
```

- [ ] **Step 6: Pasar `lang` desde las páginas**

En `src/pages/[...locale]/index.astro`, agregar `lang={lang}` a los tres usos: el
`<TrailerCard … />` y los dos `<NewsCard … />` (variante `propio` y variante `agregado`).

En `src/pages/[...locale]/noticias/index.astro`, agregar `lang={lang}` a los **cuatro**
`<NewsCard … />` (dos en la grilla visible, dos en `#news-grid-rest`).

- [ ] **Step 7: Checkpoint**

Verificación sugerida:

```bash
pnpm exec astro check     # detecta cualquier NewsCard/TrailerCard sin lang
pnpm exec astro build
grep -o 'GTA VI Countdown' dist/client/en/index.html   # hero en inglés
grep -o 'Latest news' dist/client/en/index.html
```

Commit sugerido: `feat(i18n): traducir hero, footer, donaciones y tarjetas`

---

## Task 7: Traducir los componentes con estado en cliente

Estos dos tienen su texto dentro de `<script>`, y un `<script>` de Astro no puede
importar del frontmatter. Los strings viajan por atributos `data-*` del elemento sobre
el que el script ya opera.

**Files:**
- Modify: `src/components/Waitlist.astro`, `src/components/MusicToggle.astro`
- Modify: `src/lib/waitlist.js`
- Test: `src/lib/waitlist.test.js`

**Interfaces:**
- Consumes: `getLangFromUrl`, `useTranslations` (Task 1).
- Produces: `formatCount(n, lang, labels)` — **cambia la firma**. `labels` es `{ one: string, other: string }`.

- [ ] **Step 1: Escribir el test de `formatCount` (falla)**

En `src/lib/waitlist.test.js`, reemplazar el bloque `describe('formatCount', …)` existente
por:

```js
describe('formatCount', () => {
  const es = { one: 'persona ya se sumó', other: 'personas ya se sumaron' };
  const en = { one: 'person has joined', other: 'people have joined' };

  it('formatea el número según el idioma', () => {
    expect(formatCount(1234, 'es', es).value).toBe('1.234');
    expect(formatCount(1234, 'en', en).value).toBe('1,234');
  });

  it('elige singular o plural', () => {
    expect(formatCount(1, 'es', es).label).toBe('persona ya se sumó');
    expect(formatCount(2, 'es', es).label).toBe('personas ya se sumaron');
    expect(formatCount(1, 'en', en).label).toBe('person has joined');
    expect(formatCount(0, 'en', en).label).toBe('people have joined');
  });

  // El idioma llega de un atributo data-* del DOM, así que un valor
  // inesperado no debe romper el contador.
  it('cae al idioma por defecto si el código no se reconoce', () => {
    expect(formatCount(1234, 'zz', es).value).toBe('1.234');
  });
});
```

- [ ] **Step 2: Verificar que falla**

Comando: `pnpm vitest run src/lib/waitlist.test.js`
Esperado: FAIL — `formatCount` todavía ignora los dos argumentos nuevos.

- [ ] **Step 3: Implementar `formatCount`**

En `src/lib/waitlist.js`, reemplazar la constante `COUNT_FORMAT` y la función `formatCount`
por:

```js
// Un formateador por idioma: el separador de miles cambia (1.234 vs 1,234).
const COUNT_FORMATS = {
  es: new Intl.NumberFormat('es-CO'),
  en: new Intl.NumberFormat('en-US'),
};

// Las etiquetas llegan como parámetro, no desde el diccionario: este módulo es
// una librería y no debe depender de la capa de UI. Quien lo llama las lee de
// los atributos data-* que el componente renderizó.
// Returned in two pieces because the section styles them differently: the
// number carries the GTA outline, the caption the softer variant.
export function formatCount(n, lang, labels) {
  const format = COUNT_FORMATS[lang] ?? COUNT_FORMATS.es;
  return {
    value: format.format(n),
    label: n === 1 ? labels.one : labels.other,
  };
}
```

- [ ] **Step 4: Waitlist — pasar los textos por `data-*`**

En `src/components/Waitlist.astro`, agregar al frontmatter:

```astro
---
// src/components/Waitlist.astro
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);
---
```

En el marcado, el encabezado y el subtítulo:

```astro
	<h2 class="text-2xl font-black uppercase">{t('waitlist.heading')}</h2>
	<p class="mt-4 text-white/70">{t('waitlist.sub')}</p>
```

El contador, con las etiquetas de singular/plural y el idioma colgados del contenedor:

```astro
	<div
		class="mt-8"
		data-waitlist-count
		data-lang={lang}
		data-count-one={t('waitlist.countOne')}
		data-count-other={t('waitlist.countOther')}
		aria-live="polite"
		hidden
	>
		<p class="gta-outline text-5xl font-black tabular-nums" data-waitlist-value>—</p>
		<p class="gta-outline-soft mt-1 text-xs uppercase tracking-widest text-white/80" data-waitlist-label>
			{t('waitlist.countOther')}
		</p>
	</div>
```

Y el botón, con sus tres estados y el mensaje de error:

```astro
	<button
		id="waitlist-button"
		type="button"
		data-state="idle"
		data-label-idle={t('waitlist.cta')}
		data-label-sending={t('waitlist.sending')}
		data-label-joined={t('waitlist.joined')}
		data-label-error={t('waitlist.error')}
		disabled
		class="mt-8 rounded-full border border-white/20 px-8 py-3 text-sm font-black uppercase tracking-wide transition-colors hover:border-gta-pink hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white disabled:cursor-not-allowed data-[state=joined]:border-gta-pink"
	>
		{t('waitlist.cta')}
	</button>
```

- [ ] **Step 5: Waitlist — leer los `data-*` en el script**

En el `<script>` de `src/components/Waitlist.astro`, después de la línea que declara
`errorEl`, agregar:

```ts
		// Los textos vienen del DOM porque un <script> de Astro no puede importar
		// del frontmatter donde vive el diccionario.
		const labels = {
			idle: button.dataset.labelIdle!,
			sending: button.dataset.labelSending!,
			joined: button.dataset.labelJoined!,
			error: button.dataset.labelError!,
		};
		const lang = counter.dataset.lang!;
		const countLabels = { one: counter.dataset.countOne!, other: counter.dataset.countOther! };
```

Reemplazar `render` por:

```ts
		function render(state: 'idle' | 'sending' | 'joined') {
			button!.dataset.state = state;
			button!.disabled = state !== 'idle';
			button!.textContent = labels[state];
		}
```

Reemplazar la línea de `formatCount` dentro de `showCount` por:

```ts
			const { value, label } = formatCount(count, lang, countLabels);
```

Y el mensaje de error del `catch` del listener:

```ts
				errorEl.textContent = labels.error;
```

- [ ] **Step 6: MusicToggle**

En `src/components/MusicToggle.astro`, agregar al frontmatter:

```astro
---
// src/components/MusicToggle.astro
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url.pathname);
const t = useTranslations(lang);
---
```

En el `<button>`, reemplazar los atributos de texto y agregar los `data-*`:

```astro
	aria-label={t('music.enable')}
	title={t('music.title')}
	data-label-enable={t('music.enable')}
	data-label-mute={t('music.mute')}
	data-label-on={t('music.on')}
	data-label-off={t('music.off')}
```

Y el `<span>` interno:

```astro
	<span class="sr-only" data-music-label>{t('music.off')}</span>
```

En el `<script>`, después de la línea que declara `label`, agregar:

```ts
		const text = {
			enable: button.dataset.labelEnable!,
			mute: button.dataset.labelMute!,
			on: button.dataset.labelOn!,
			off: button.dataset.labelOff!,
		};
```

Y reemplazar `render` por:

```ts
		function render(playing: boolean) {
			button!.setAttribute('aria-pressed', String(playing));
			button!.setAttribute('aria-label', playing ? text.mute : text.enable);
			offIcon.classList.toggle('hidden', playing);
			onIcon.classList.toggle('hidden', !playing);
			label.textContent = playing ? text.on : text.off;
		}
```

- [ ] **Step 7: Checkpoint**

Verificación sugerida:

```bash
pnpm vitest run
pnpm exec astro build
```

Y a mano en `/en/`: el botón dice `I'm in`, el toggle de música anuncia `Music off`,
y el contador de la lista de espera usa coma como separador de miles.

Commit sugerido: `feat(i18n): traducir lista de espera y toggle de música`

---

## Task 8: 404 bilingüe y documentación

**Files:**
- Modify: `src/pages/404.astro`
- Modify: `AGENTS.md`

**Interfaces:**
- Consumes: nada. El 404 no usa el diccionario a propósito: muestra los dos idiomas a la vez.
- Produces: nada.

- [ ] **Step 1: 404 bilingüe**

`src/pages/404.astro` completo:

```astro
---
// src/pages/404.astro
//
// Bilingüe y sin diccionario: Vercel sirve este único 404.html para cualquier
// URL inexistente, incluidas las de /en/, así que la página no puede saber en
// qué idioma estaba el visitante. Mostrar los dos es más honesto que adivinar.
import Layout from '../layouts/Layout.astro';
---

<Layout title="Página no encontrada — Page not found" description="La página que buscas no existe. — The page you're looking for doesn't exist.">
	<section class="mx-auto max-w-2xl px-4 pt-28 pb-20 text-center">
		<h1 class="gta-outline text-5xl font-black uppercase">404</h1>
		<p class="mt-4 text-white/70">Esta página no existe o se movió de lugar.</p>
		<p class="mt-1 text-white/50" lang="en">This page doesn't exist or has moved.</p>
		<div class="mt-8 flex flex-wrap justify-center gap-3">
			<a
				href="/"
				class="inline-block rounded-full border border-white/20 px-8 py-3 text-sm font-black uppercase tracking-wide transition-colors hover:border-gta-pink hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
			>
				Volver al inicio
			</a>
			<a
				href="/en/"
				lang="en"
				class="inline-block rounded-full border border-white/20 px-8 py-3 text-sm font-black uppercase tracking-wide transition-colors hover:border-gta-pink hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
			>
				Back home
			</a>
		</div>
	</section>
</Layout>
```

- [ ] **Step 2: Documentar la convención**

En `AGENTS.md`, agregar esta sección entre "Variables de entorno" y "Documentation":

```markdown
## Idiomas

El sitio es bilingüe: español sin prefijo (`/noticias`) e inglés bajo `/en/`
(`/en/noticias`). Las tres páginas se generan desde `src/pages/[...locale]/`, donde
`locale: undefined` produce la ruta española y `locale: 'en'` la inglesa.

- **Todo el texto de UI vive en `src/i18n/ui.ts`.** El objeto `en` se valida contra las
  claves de `es` con `satisfies`, así que una clave que falte o sobre en inglés es un
  error que `astro check` detecta. No hardcodees texto en los componentes.
- **El idioma se deriva siempre de la URL** con `getLangFromUrl(Astro.url.pathname)`,
  nunca de una prop ni de `Astro.currentLocale`. Un solo camino de código, con tests en
  `src/i18n/utils.test.ts`.
- **Los `<script>` de Astro no pueden importar del frontmatter.** Los textos que necesita
  el JS del cliente se pasan por atributos `data-*` en el elemento sobre el que el script
  ya opera. Ver `Waitlist.astro` y `MusicToggle.astro`.
- **Los artículos van en `src/content/articulos/es/` o `en/`**, y las imágenes compartidas
  en `_assets/`. El idioma sale del directorio, no del frontmatter; un `.md` fuera de un
  directorio de idioma rompe el build a propósito. Para emparejar dos artículos con slugs
  distintos, usa `translationKey` en el que no coincide.
- **Detección de idioma:** un script inline en el `<head>` del Layout, solo en `/`.
  Redirige a `/en/` si el navegador pide inglés y no hay preferencia guardada. Solo el
  switch escribe `localStorage['gta6:lang']`; la detección nunca escribe.
```

- [ ] **Step 3: Checkpoint**

Verificación sugerida:

```bash
pnpm vitest run
pnpm exec astro check
pnpm exec astro build
```

Commit sugerido: `feat(i18n): 404 bilingüe y documentar la convención de idiomas`

---

## Verificación final (la ejecuta el dueño del repo)

```bash
pnpm vitest run
pnpm exec astro check
pnpm exec astro build
```

Rutas que deben existir en `dist/client/`:

```
index.html
en/index.html
noticias/index.html
en/noticias/index.html
noticias/bienvenida/index.html
en/noticias/welcome/index.html
404.html
```

Comprobaciones manuales:

- `/` con navegador en inglés y sin `gta6:lang` → redirige a `/en/`.
- Usar el switch → `gta6:lang` queda guardado y `/` deja de redirigir.
- `/noticias` con navegador en inglés → **no** redirige.
- El switch en `/noticias/bienvenida` lleva a `/en/noticias/welcome`.
- `dist/client/404.html` no contiene `hreflang` ni `canonical`.
