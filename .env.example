# Credenciales de Upstash Redis para la lista de espera (/api/waitlist).
# Se provisionan desde el Vercel Marketplace con `vercel integration add`.
# Sin ellas el sitio compila igual y /api/waitlist responde 503.
KV_REST_API_TOKEN=
KV_REST_API_URL=